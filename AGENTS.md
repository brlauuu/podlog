# Working agreements

- For full repository audits, prefer the `nightly-audit` skill when available.
- Unattended audits must not modify source files, commit, push, or create issues unless explicitly requested.
- Prioritize correctness, regressions, stale docs, dead code, dependency health, and test gaps.
- Always include file paths and evidence for findings.
- For any GitHub comment, review, or code review feedback posted by Codex, sign the message with:
  `Posted by Codex`
  `Model: GPT-5-based coding agent`
